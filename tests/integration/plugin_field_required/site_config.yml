---
default_output: dummy
plugins:
  dummy:
    # Enable 'dummy' plugin, which is disabled by default
    enabled: true
    fields:
      test:
        parent2:
          type: list
          subtype: dict
          fields:
            field1:
              type: str
              required: true
            field2:
              type: str
