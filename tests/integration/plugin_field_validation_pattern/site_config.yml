---
default_output: dummy
plugins:
  dummy:
    # Enable 'dummy' plugin, which is disabled by default
    enabled: true
    fields:
      test:
        some_field:
          type: str
          required: true
          validation_pattern: '[A-Z][0-9][a-z]'
